# pnpm configuration
# Testing: Using default isolated mode (symlinks) for better dependency management
# node-linker=hoisted  # ← Commented out for testing

# Note: With isolated mode (default):
# - Local: Uses symlinks (faster installs, strict dependencies)
# - Docker: Next.js standalone handles this automatically
# - Benefit: No phantom dependencies, better security
